
.onAttach <- function(libname, pkgname) {
  packageStartupMessage("Welcome to LearningTower to access the entire student data for each year run the following code.
Repeat the same code similarly for the desired years.
student_data_2000 <- load_student(2000)")
}
